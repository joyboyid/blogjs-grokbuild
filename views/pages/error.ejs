<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <!-- Simple top nav -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">BlogJS</a>
            <div>
                <a class="nav-link text-light d-inline me-3" href="/about">About</a>
                <a class="nav-link text-light d-inline me-3" href="/contact">Contact Us</a>
            </div>
        </div>
    </nav>

    <div class="container text-center mt-5">
        <div style="padding-top: 60px;">
        <h1 class="display-4 text-danger">Oops!</h1>
        <h3 class="mb-3">Something went wrong</h3>
        <p class="text-muted"><%= message %></p>
        
        <% if (process.env.NODE_ENV === 'development' && error) { %>
            <pre class="text-start bg-white p-3 rounded border text-danger small" style="max-height: 300px; overflow:auto;">
                <%= error.stack || error %>
            </pre>
        <% } %>
        
        <a href="/" class="btn btn-primary mt-3">Back to Home</a>
        </div>
    </div>
</body>
</html>
